<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".screen.AddAnimeFragment">

    <include
        android:id="@id/top_app_bar"
        layout="@layout/layout_action_bar"/>

    <LinearLayout
        android:layout_marginHorizontal="@dimen/spacing_10dp"
        android:layout_marginVertical="@dimen/spacing_16dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        app:layout_constraintTop_toBottomOf="@id/top_app_bar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <ImageView
                android:id="@+id/profile_preview"
                android:layout_width="match_parent"
                android:layout_height="@dimen/anime_card_height" />

            <Button
                android:id="@+id/add_cover_button"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Select image +"/>

        </LinearLayout>

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <EditText
                android:id="@+id/edit_english_name"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:hint="Anime Name: (EN)"
                android:inputType="textAutoComplete"
                android:autofillHints="Enter your Anime Name: (EN)" />

            <EditText
                android:id="@+id/edit_chinese_name"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:hint="Anime Name: (CH)"
                android:inputType="textAutoComplete"
                android:autofillHints="Enter your Anime Name: (CH)" />

        </LinearLayout>

    </LinearLayout>



</androidx.constraintlayout.widget.ConstraintLayout>